# OS / IDE (운영체제 및 IDE 자동생성 파일)
.DS_Store
Thumbs.db
.vscode/
.idea/

# env (환경변수 파일)
.env
.env.*
!.env.example

# logs (로그 파일)
*.log

# docker volumes (Docker 로컬 데이터 볼륨)
**/data/
**/volumes/

# secrets (민감한 보안 정보 - YAML/JSON 형식)
secrets.yml
secret.yaml
secrets/
*.pem          # 개인키 & 공개키
*.key          # 암호화 키
*.crt          # SSL/TLS 인증서
*.pfx          # Windows 인증서
*.p12          # PKCS12 인증서

# credentials & config (자격증명 및 설정파일)
.kube/         # Kubernetes config 디렉토리
kubeconfig     # K8s 인증 파일
.aws/          # AWS credentials 디렉토리
.ssh/          # SSH 개인키 디렉토리
*password*     # 비밀번호 포함 파일
*credential*   # 자격증명 파일
*api_key*      # API 키 파일
*token*        # 토큰 파일
config         # 민감한 설정파일
.env.local     # 로컬 환경변수 (개발용)
.env.*.local   # 환경별 로컬 설정 (스테이징, 프로덕션 등)

# domains & addresses (도메인 및 주소 정보)
*.domain       # 도메인 설정 파일
hosts.txt      # hosts 파일 (IP-도메인 매핑)
dns.conf       # DNS 설정 파일